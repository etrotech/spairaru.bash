#!/usr/bin/env bash
# ######################################################################################
# HELP
# ######################################################################################
printf "

${ET_TTL} ${S_TTL} v${S_VER}

${ANSI_F_GR}- https://micah-t.xyz.com/app/m.sh ${_ANSI}
${ANSI_F_GR}- https://github.com/micah0912/m.sh ${_ANSI}


${ANSI_F_GR}Usage ----------------------------------------------------------------------------------${_ANSI}

${S_PPT_ITND}${S_S_CLI}
${S_PPT_ITND}${S_S_CLI} <action>
${S_PPT_ITND}${S_S_CLI} <action> [parameters ...]
${S_PPT_ITND}${S_S_CLI} <library> <method>
${S_PPT_ITND}${S_S_CLI} <library> <method> [parameters ...]
${S_PPT_ITND}${S_S_CLI} [parameters ...]

" ;


return $tru ;
